let sentence = "Check out the 10 Green Building Principles that can get real estate to net zero. The real estate industry has a key role to play in bringing down carbon emissions.but too few organizations currently have sustainability action plans.";

// ============> split returns an array
let res1 = sentence.split(' '); //---------->separate every word (word);
let res2 = sentence.split('.'); //----------> separate every sentence (sentence);
let res3 = sentence.split(''); //---------->separate every latter (letter);
console.log(res1)
console.log(res2)